[package]
name = "persistence-postgres"
version = "0.1.0"
edition = "2021"

[dependencies]
qa-core = { path = "../qa-core" }
postgres = "0.19"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[dev-dependencies]
